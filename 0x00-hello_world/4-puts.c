#include <stdio.h>

/**
 * main - prints a string of text.
 * Description: Prints text in text[] array using puts to stdout.
 * Return: "Programming is like building a multilingual puzzle"
 */
int main(void)
{
	char text[] = {"\"Programming is like building a multilingual puzzle"};

	puts(text);
	return (0);
}
