#include <stdio.h>

/**
 * main - prints out text.
 * Return: prints out "with proper grammar, but the outcome is a piece of art,"
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
