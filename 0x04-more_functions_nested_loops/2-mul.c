#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Description: multiplies two integers and returns the result
 *
 * Return: returns prod on success
 */
int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
