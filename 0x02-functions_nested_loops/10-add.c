#include "main.h"

/**
 * add - adds two integers
 * @x: first integer
 * @y: second integer
 * Description: adds two integers and returns the result
 *
 * Return: returns sum on success
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
